------jquery------

$("#head1") // 같음
document.querySelector("#head1") // 같음

(https://api.jquery.com/ => jquery 메소드 선택하여 사용)
검색 사용 시 removed 되어있는지 버전 확인

$("#id").method()

전역객체
function(){
   alert("hello");
}
window.func()
객체.속성
객체 window
속성 func()
속성이 함수 : method
this 는 함수 내부에서 사용하는 변수(가변적)
